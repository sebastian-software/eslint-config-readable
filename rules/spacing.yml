rules:
  # Enforce spaces inside of brackets
  array-bracket-spacing:
    - error
    - always
    - objectsInArrays: false

  spaced-comment:
    - error
    - always
    - exceptions: [ "*", "=", "-" ]
